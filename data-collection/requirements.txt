# 공고 수집 배치 전용. 추천 화면·모델 학습 패키지는 넣지 않는다.
#
#   python -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt

# ── 수집 ────────────────────────────────────────────────
# urllib 로도 되지만 첨부 다운로드에서 스트리밍·타임아웃 처리에 쓴다.
requests>=2.31

# ── 저장 ────────────────────────────────────────────────
# [rsa] 는 MySQL 8 의 caching_sha2_password 인증에 필요하다.
PyMySQL[rsa]>=1.1,<2

# ── 첨부 문서 텍스트 추출 ─────────────────────────────────
# HWPX·DOCX 는 표준 라이브러리(zipfile)만으로 처리한다.
pdfplumber>=0.11        # PDF
olefile>=0.47           # 구 HWP (5.0)

# ── 공고 요약 임베딩 · 벡터 검색 ──────────────────────────
# --skip-embed 로 끄면 아래 셋은 없어도 배치가 돈다.
# torch 는 CPU 판이 기본이다. GPU 를 쓰려면 따로 설치한다.
numpy>=1.26
sentence-transformers>=3.0    # BGE-M3 로 공고·질의를 벡터로
chromadb>=1.0                 # 벡터 색인
