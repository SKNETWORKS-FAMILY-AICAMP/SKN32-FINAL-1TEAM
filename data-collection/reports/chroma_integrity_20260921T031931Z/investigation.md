# 불일치 원인 조사 — 사람이 쓴 메모 (자동 생성 아님)

2026-09-21 · Claude · 읽기 전용 조사. 이 폴더의 `summary.md`·`integrity.json` 은 검사 도구가 만든 것이고,
이 파일은 그 결과를 보고 따로 조사한 내용이다.

## 1. `kstartup:179193` — 실제 불일치 (1건)

공고: 2026년 한전KPS 창업벤처기업 육성 지원사업 참여기업 모집안내(연장)

| 확인한 것 | 결과 |
|---|---|
| DB 현재 내용 ↔ NPZ 입력 해시 | **같다** (NPZ 벡터는 지금 공고 내용으로 만든 것) |
| NPZ 벡터 ↔ Chroma 벡터 | **다르다** — 최대 원소 차이 0.0193, 코사인 0.9866 |
| 벡터 길이 | NPZ 1.0000001 · Chroma 1.0 (둘 다 정상 벡터) |
| DB `created_at` | 2026-09-10 00:56 |
| DB `embedding_updated_at` | 2026-09-14 00:45 |
| DB `updated_at` | 2026-09-15 00:01 (제목의 "(연장)"으로 보아 기간 연장 수정으로 보인다 — 추정) |
| `data/collect_log.jsonl` 24건의 `changed_ids` | 이 공고는 **한 번도 없다** |
| 오늘(9/21) 배치가 Chroma 에 넣은 44건 | 이 공고 없음 |

판단: **Chroma 가 이 공고의 옛(또는 다른 시점의) 벡터를 들고 있을 가능성이 높다.**
어느 경로로 NPZ 만 갱신되고 Chroma 는 그대로였는지는 기록만으로 **확정하지 못했다.**
배치 로그에 이 공고의 벡터 재생성이 남아 있지 않으므로, 로그 밖(수동 임베딩 실행 등)에서 NPZ 가 바뀌었을 수 있다.

영향: 의미 검색(dense)에서 이 공고는 코사인 0.987 만큼 다른 벡터로 순위가 매겨진다. 작은 차이지만 실제 차이다.
하이브리드의 BM25 쪽은 DB 내용과 맞다(기존 검사에서 BM25 불일치 0건).

## 2. 비트가 다른 552건 — 정상

552건 모두 NPZ 벡터를 길이 1로 다시 나누면 Chroma 값과 같다(최대 차이 1.5e-8).
코사인 거리 색인은 저장할 때 벡터를 다시 정규화하고, NPZ 벡터의 길이가 float32 오차로 1±1e-7 이라
마지막 자리가 바뀐다. 허용 오차(1e-6)는 결과를 보기 전에 정한 값 그대로다.

## 3. 고치지 않았다

지시서 범위상 색인 재생성·upsert 는 하지 않았다. 고치려면 사용자 승인 뒤 한 건만 다시 넣으면 된다.

```
.\.venv\Scripts\python.exe -X utf8 -c "from search import vecstore; print(vecstore.sync(['kstartup:179193']))"
```

`vecstore.sync` 는 NPZ 벡터를 그 ID 로 upsert 한다(Chroma **쓰기**). 실행 뒤 `--check-only` 로 다시 확인한다.
EC2 에 올라간 벡터(`vector_upload`)도 같은 문제가 있는지는 이번에 확인하지 않았다.

## 4. 후속 — 재동기화 (2026-09-21, 사용자 승인)

- 백업: `data/vecstore/chroma_backup_20260921T_before_179193_sync/` (원본과 파일 6개 지문 일치 확인, 지문은 같은 이름의 `_fingerprint.json`).
- 실행: `vecstore.sync(['kstartup:179193'])` → upsert 1건 · 색인 2,328건 · 벡터 파일 2,328건.
- 재검사: `reports/chroma_integrity_20260921T050045Z/` **통과**, `chroma_content_verified = True`.
- 백업과 대조: 바뀐 벡터는 `kstartup:179193` 한 건뿐. 나머지 2,327건은 바이트까지 같고 컬렉션 메타데이터도 같다.
