VITE_GOOGLE_CLIENT_ID=
# 백엔드(FastAPI) 주소. 세션 쿠키 SameSite 때문에 프론트도 localhost로 접속해야 함
# (127.0.0.1로 열면 로그인은 되는데 그 다음 요청에 쿠키가 안 실려감 — back/app/security.py 참고.
# 이 값도 반드시 localhost로 맞춰야 한다 — 127.0.0.1로 두면 프론트가 localhost로 열려있어도
# 백엔드 쪽만 다른 site 취급돼서 똑같은 문제가 재발한다).
VITE_API_BASE=http://localhost:8000
