{
  "name": "s-brain-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost --configLoader native",
    "build": "vite build --configLoader native",
    "preview": "vite preview --host localhost --configLoader native",
    "check:render": "node scripts/render-check.mjs",
    "standalone": "npm run build && node scripts/standalone.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "zustand": "^5.0.15"
  },
  "devDependencies": {
    "autoprefixer": "10.4.21",
    "postcss": "8.5.6",
    "tailwindcss": "3.4.17",
    "vite": "8.2.2"
  }
}
